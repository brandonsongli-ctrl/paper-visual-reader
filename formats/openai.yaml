interface:
  display_name: "Paper Visual Reader"
  short_description: "Visual digest with OCR, claim filters, and strict evidence gating"
  default_prompt: "Use $paper-visual-reader to build a bilingual visual digest with OCR fallback, claim-level evidence ledger, and strict anti-hallucination checks."

policy:
  allow_implicit_invocation: true
